<%= form_with(model: vendor_follower) do |form| %>
  <% if vendor_follower.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vendor_follower.errors.count, "error") %> prohibited this vendor_follower from being saved:</h2>

      <ul>
        <% vendor_follower.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :vendor_id, style: "display: block" %>
    <%= form.text_field :vendor_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
